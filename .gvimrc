" GVim Settings {{{
" maybe i'll relocate it to .gvimrc later
    set novisualbell 
    set t_vb=
    set guioptions=Aci
    "colorscheme wombat256mod
    " Win32 Settings {{{
    if has('win32')
        set guifont=Consolas:h10:cANSI
    endif
    " }}}

" }}}

