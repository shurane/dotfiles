
" GVim Settings {{{
" maybe i'll relocate it to .gvimrc later
    set visualbell t_vb=
    set guioptions=Aci
    colorscheme wombat
    " Win32 Settings {{{
    if has('win32')
        set guifont=Consolas:h10:cANSI
    endif
    " }}}

" }}}

