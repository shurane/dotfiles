services:
  glance:
    image: glanceapp/glance:latest
    restart: unless-stopped
    environment:
      TZ: America/Los_Angeles
    networks:
      - proxy-tier
    volumes:
      - ./config:/app/config
      - ./assets:/app/assets
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "-T", "5", "http://127.0.0.1:8080"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    labels:
      caddy_0: http://${HOSTNAME_BASE:?err}, http://${HOSTNAME_ALIAS_BASE:-m.lan}
      caddy_0.reverse_proxy: "{{upstreams 8080}}"
      caddy_1: https://${HOSTNAME_BASE:?err}, https://${HOSTNAME_ALIAS_BASE:-m.lan}
      caddy_1.tls: internal
      caddy_1.reverse_proxy: "{{upstreams 8080}}"

networks:
  proxy-tier:
    external: true
